# 快速开始指南

## 🎯 新功能概览

本次更新为记账列表页面添加了两个强大的新功能：

### 1️⃣ 月份快速选择器
**一句话介绍**: 点击月份标题，快速跳转到任意月份查看记账

### 2️⃣ 日历热力图  
**一句话介绍**: 可视化展示每日收支，消费模式一目了然

---

## 🚀 5分钟体验

### Step 1: 打开记账列表
在应用中进入"记账列表"页面

### Step 2: 体验月份选择器
1. 点击顶部的月份标题（如 "2024年11月 ▼"）
2. 在弹出的选择器中：
   - 横向滑动选择年份
   - 点击月份卡片跳转
   - 或点击"本月"快速返回

### Step 3: 查看热力图
1. 在筛选器下方查看日历热力图
2. 观察不同日期的颜色深浅（颜色越深=消费越多）
3. 查看底部的月度汇总数据

### Step 4: 收起/展开
点击"收起热力图"按钮，节省屏幕空间

---

## 📖 核心概念

### 热度等级
| 金额范围 | 热度等级 | 颜色 |
|---------|---------|------|
| 0元 | Level 0 | 背景色（无消费） |
| 1-99元 | Level 1 | 浅蓝紫 |
| 100-499元 | Level 2 | 中蓝紫 |
| 500-999元 | Level 3 | 深蓝紫 |
| 1000元+ | Level 4 | 超深蓝紫 |

### 视觉标识
- **蓝色边框** = 今天
- **灰色半透明** = 未来日期（不可选）
- **小圆点** = 当前月份
- **数字角标** = 交易笔数

---

## 🎨 设计理念

### 参考对象
1. **Google Calendar**: 简洁的月份选择
2. **Telegram**: 优雅的底部抽屉
3. **GitHub**: 经典的贡献热力图

### 设计原则
- **简洁优雅**: 不过度设计
- **信息密度**: 在有限空间展示丰富信息
- **即时反馈**: 点击立即响应
- **智能防呆**: 未来日期自动灰显

---

## 💻 开发者信息

### 新增文件
```
src/components/transaction/
├── MonthPickerSheet.tsx          # 月份选择器组件
└── DailyStatisticsCalendar.tsx   # 日历热力图组件
```

### 修改文件
```
src/screens/
└── TransactionListScreen.tsx     # 集成页面
```

### 依赖项
- React Native
- TypeScript
- Ionicons
- React Navigation

### 代码量统计
- MonthPickerSheet: ~400 行
- DailyStatisticsCalendar: ~500 行
- TransactionListScreen 修改: ~50 行

---

## 🔧 配置说明

### 主题配置
所有颜色、间距、圆角都从 `src/constants/theme.ts` 读取，保持全局一致。

### 自定义热度阈值
可在 `DailyStatisticsCalendar.tsx` 中修改 `getHeatLevel` 函数：

```typescript
const getHeatLevel = (stat: DailyStatistic | null): number => {
    if (!stat || stat.count === 0) return 0;
    const total = Math.abs(stat.expense) + Math.abs(stat.income);
    
    // 自定义阈值
    if (total < 100) return 1;
    if (total < 500) return 2;
    if (total < 1000) return 3;
    return 4;
};
```

---

## 📚 相关文档

- [完整功能说明](./MONTH_PICKER_AND_HEATMAP_FEATURE.md)
- [功能演示指南](./FEATURE_DEMO_GUIDE.md)
- [测试数据](./src/components/transaction/__tests__/MonthPickerAndCalendar.test.ts)

---

## ❓ 常见问题

### Q1: 为什么未来月份不能选？
**A**: 为了防止用户误操作，未来月份被设置为不可选状态。

### Q2: 热力图可以隐藏吗？
**A**: 可以！点击"收起热力图"按钮即可。

### Q3: 如何定义"大额消费"？
**A**: 默认1000元以上为大额，可在代码中自定义阈值。

### Q4: 点击热力图的日期会怎样？
**A**: 目前会在控制台输出日期，后续可扩展为滚动到对应交易。

### Q5: 支持导出热力图吗？
**A**: 暂不支持，已列入后续扩展计划。

---

## 🎉 开始使用

现在您可以运行应用，体验这些新功能了！

```bash
# iOS
npm run ios

# Android  
npm run android
```

**祝您使用愉快！**
